// PUBLISH_CHANNEL_CONTENT: Kanala tek bir mesaj gönderip kanaldaki tüm kullanıcılara dağıtan aksiyon.
channelContents:

client {
      PublishChannelContentDoor.js {
                  PublishChannelContentOut(content, token)
      }
   } -(content, token) ->
gateway -(content, token) ->
persist -(content, token) ->
notification-(content)->
client{
      PublishChannelContentDoor.js {
                  PublishChannelContentIn(content)
      }
}