// TRANSACTION_DIALOG: Herhangi bir kişiye Thankcoin gönderip, değişikliği tüm kullanıcılara dağıtan aksiyon.
loginUser:

client {
      TransactionDialogDoor.js {
                  transactionDialogOut(transaction, token)
      }
   } -(content, token) ->
gateway -(transaction, token) ->
persist -(transaction, token) ->
notification-(transaction)->
client{
      TransactionDialogDoor.js {
                  transactionDialogIn(transaction)
      }
}